# Python Backend Workspace

This directory is reserved for Python services, scripts, or ML utilities.

## Suggested setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -U pip
```

Add your Python modules and dependency files here (for example, `requirements.txt` or `pyproject.toml`).
